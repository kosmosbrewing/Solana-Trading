# 2026-03-31 Handoff — Post-Patch Live Canary Review

## Summary

- historical canary:
  - `2026-03-30 09:46:50 UTC` ~ `2026-03-30 21:56 UTC` (`~12.2h`)
  - BUY 시그널 `14`건, 실거래 `0`건
  - 직접 blocker는 주로 `poor_execution_viability`
- extended live canary:
  - `2026-03-31 03:53:41 UTC` ~ `2026-03-31 12:35:27 UTC` (`~8.7h`)
  - 인프라 정상, BUY 시그널 `0`, 실거래 `0`
  - 이 구간의 직접 blocker는 `poor_execution_viability`가 아니라 `signal generation upstream`
- follow-up fix:
  - realtime pre-gate probe를 실제 `momentumTrigger` 주문식과 정렬
  - backtest도 v5 `execution RR` 기준(`tp1`, `0.8/1.0`)으로 정렬
  - scanner blacklist startup preload 보강
  - targeted test `46`개 통과
  - commit: `0697d07`
- operator override:
  - 기본 우선순위는 여전히 `backtest -> paper -> live`
  - 다만 `2026-03-31 12:39:24 UTC`부터 사용자 요청으로 `live-first` canary를 다시 진행 중
  - risk guard는 보수적으로 유지하고 realtime coverage만 `5 -> 8`로 확대

## 1. Historical 12.2h Canary

### rejection mix
| 사유 | 건수 |
|---|---:|
| `poor_execution_viability` | 10 |
| `edge tracker blacklist` | 3 |
| `buy_ratio < 0.65` | 1 |

### 핵심 해석
- 이 구간의 0-entry 직접 원인은 "시그널 부재"가 아니라 "시그널은 있었지만 gate/risk에서 전부 막힘"이었다.
- 특히 `poor_execution_viability`가 지배적이었고, 당시 후속 패치의 직접 배경이 됐다.

## 2. Completed Fixes

### Patch A. pre-gate execution viability probe 정합성 수정
- SOL notionals를 token quantity로 환산한 뒤 RR을 계산하도록 보정
- pre-gate probe와 실제 주문 단위를 맞췄다.

### Patch B. pre-gate / post-size telemetry 분리
- `execution.preGate`
- `execution.postSize`

각 snapshot에 아래가 남도록 보강:
- `effectiveRR`
- `roundTripCost`
- `riskPct`
- `rewardPct`
- `entryPriceImpactPct`
- `exitPriceImpactPct`
- `quantity`
- `notionalSol`
- `rejected`
- `sizeMultiplier`

### Patch C. true post-size recomputation
- `0.5x` haircut 이후 수량으로 다시 계산한 RR이 저장되도록 수정
- audit / position signal trace에도 post-size snapshot이 남도록 보강

## 3. Extended Live Canary (`2026-03-31 03:53:41` ~ `12:35:27 UTC`)

### facts
- 시작 로그:
  - live boot at `2026-03-31 03:53:41.987 UTC`
  - preflight는 실패했지만 `enforceGate=false`로 live 진행
- 종료는 env 변경에 따른 operator restart였다.
- 이 구간에서 확인된 것:

| 항목 | 건수 |
|---|---:|
| `Signal: BUY` | 0 |
| `Signal filtered` | 0 |
| `Trade opened` | 0 |
| `Positions` | 0 유지 |
| `Daily PnL` | `0.0000 SOL` 유지 |

### runtime diagnostics snapshot
| 항목 | 건수 |
|---|---:|
| `realtime_candidate_seen` | 65 |
| unique candidate token | 26 |
| `pre_watchlist_reject` | 272 |
| `rate_limit` | 32 |
| `admission_skip` | 6 |

### pre-watchlist reject breakdown
| 기준 | `unsupported_dex` | `non_sol_quote` |
|---|---:|---:|
| event count | 217 | 55 |
| unique token | 3 | 43 |

### 해석
- event 빈도로는 `unsupported_dex`가 커 보이지만, 실제 후보 폭을 줄인 구조적 손실은 `non_sol_quote`가 더 크다.
- 즉 `ANIME/meteora` 같은 반복 reject는 noisy churn이고, 실제 retained universe를 좁히는 건 SOL quote 미스매치였다.

## 4. What Changed In Interpretation

### historical blocker와 최신 blocker는 다르다
- `2026-03-30`의 12.2h canary direct blocker는 `poor_execution_viability`였다.
- 하지만 `2026-03-31` extended live canary에서는 BUY 시그널 자체가 `0`이었다.
- 따라서 최신 해석은 "gate가 전부 막았다"가 아니라 "gate 이전 단계에서 표본이 사라졌다"다.

### 이번 0-signal은 trigger default만으로 설명되지 않는다
- 코드 default는 문서보다 빡센 부분이 남아 있다.
- 다만 실제 runtime env는 이미 비교적 느슨했다:
  - `REALTIME_VOLUME_SURGE_MULTIPLIER=2.0`
  - `REALTIME_CONFIRM_MIN_BARS=1`
  - `REALTIME_CONFIRM_MIN_CHANGE_PCT=0.005`
- 따라서 이번 8.7h 0-signal을 단순히 "trigger threshold가 너무 엄격해서"라고만 보긴 어렵다.

### 더 직접적인 의심 지점
1. upstream candidate quality 손실
2. `unsupported_dex` / `non_sol_quote` 반복 churn
3. watchlist 8 대비 realtime subscription cap 5였던 input coverage 제약
4. admission skip / rate-limit noise

## 5. Noise Profile

- 지속 관측:
  - Gecko `429`: `32`
  - `unsupported_dex` / `non_sol_quote` 반복
  - scanner watchlist churn
- 비교적 약한 항목:
  - Helius `429`: 이 구간에서는 직접 관측되지 않음
  - realtime seed backfill failure: 현재 구간에서는 없음
  - fallback batch failure: `1`

### 중요도 해석
- 이 항목들은 여전히 운영 노이즈다.
- 다만 최신 extended run의 직접 원인은 downstream execution RR이 아니라 `BUY 표본 부재`다.

## 6. Current Live-First Override (`2026-03-31 12:39 UTC`)

기본 우선순위는 여전히 `backtest -> paper -> live`다.

다만 현재는 사용자 요청으로 live canary를 먼저 계속 본다.
이 override는 plan 자체를 뒤집는 것이 아니라, "보수 risk + widened input coverage"로 live runtime을 한 번 더 확인하는 예외 운영이다.

### active env snapshot
- `TRADING_MODE=live`
- `PREFLIGHT_ENFORCE_GATE=false`
- `MAX_WATCHLIST_SIZE=8`
- `REALTIME_MAX_SUBSCRIPTIONS=8`
- `SCANNER_REENTRY_COOLDOWN_MS=1800000`
- `REALTIME_SEED_BACKFILL_ENABLED=false`
- `REALTIME_DISABLE_SINGLE_TX_FALLBACK_ON_BATCH_UNSUPPORTED=true`
- `REALTIME_SEED_ALLOW_SINGLE_TX_FALLBACK=false`
- `MAX_RISK_PER_TRADE=0.005`
- `MAX_DAILY_LOSS=0.02`
- `MAX_CONCURRENT_ABSOLUTE=1`

### override rationale
- risk guard는 유지한다.
- startup burst 보호값도 유지한다.
- 대신 이전 extended run에서 watchlist 8 대비 realtime subscription이 `5`로 잘려 있던 점을 줄이기 위해 coverage만 확장했다.

## 7. Current Next Check

1. `Helius WS subscriptions active`가 실제로 `8`까지 올라가는지 확인
2. `candidate_seen -> pre_watchlist_reject -> admission_skip -> signal -> filtered -> trade_opened` funnel을 다시 집계
3. `Realtime admission blocked`가 새로 쌓이는지 확인
4. Gecko `429`가 input coverage 해석을 얼마나 오염시키는지 분리
5. 다음 BUY가 생기면 그때부터 다시 `execution.preGate` / `execution.postSize`를 해석

## 8. Current Judgment

```text
인프라: 정상
historical 12.2h canary: 0 entry, poor_execution_viability 지배
extended 8.7h live canary: 0 signal, upstream candidate/input coverage 문제 의심
telemetry patch: 완료
2026-03-31 follow-up fixes: 완료
current operator mode: live-first override with conservative risk

결론:
  2026-03-31 기준 핵심 질문은 더 이상
  "execution RR이 다시 다 막는가"만이 아니다.
  최신 live canary에서는 signal generation upstream이 먼저 죽고 있었고,
  현재는 보수 risk를 유지한 채 realtime input coverage를 넓혀
  그 가설을 다시 확인하는 단계다.
```
