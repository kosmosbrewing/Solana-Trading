# 2026-03-31 Handoff — Post-Patch Live Canary Review

## Summary

- 핵심 canary 구간: `2026-03-30 09:46:50 UTC` ~ `2026-03-30 21:56 UTC` (`~12.2h`)
- 결과:
  - 인프라 정상
  - 실거래 0건
  - BUY 시그널 14건 전부 필터링
- 이후 조치:
  - pre-gate execution viability 단위/telemetry 패치 완료
  - true `postSize` 비교값 저장 패치 완료
  - `2026-03-30 22:22:46 UTC`에 fresh live run 재시작

## 1. 12.2h canary의 직접 blocker

### rejection mix
| 사유 | 건수 |
|---|---:|
| `poor_execution_viability` | 10 |
| `edge tracker blacklist` | 3 |
| `buy_ratio < 0.65` | 1 |

### 핵심 해석
- 0-entry의 직접 원인은 "시그널이 없었다"가 아니라 "시그널은 있었지만 전부 gate/risk에서 막혔다"였다.
- 특히 `poor_execution_viability`가 지배적이었다.
- 다만 당시 경로는 pre-gate probe 단위/telemetry가 충분히 설명 가능하지 않았고, 이 점이 후속 패치의 직접 원인이 됐다.

## 2. 패치로 보강한 것

### Patch A. pre-gate execution viability probe 정합성 수정
- SOL notionals를 token quantity로 환산한 뒤 RR을 계산하도록 보정
- 관련 목적:
  - pre-gate probe와 실제 주문 단위를 맞춤
  - probe 기반 RR 왜곡 가능성 제거

### Patch B. pre-gate / post-size telemetry 분리
- `execution.preGate`
- `execution.postSize`

각 snapshot에 아래가 남도록 보강:
- `effectiveRR`
- `roundTripCost`
- `riskPct`
- `rewardPct`
- `entryPriceImpactPct`
- `exitPriceImpactPct`
- `quantity`
- `notionalSol`
- `rejected`
- `sizeMultiplier`

### Patch C. true post-size recomputation
- `0.5x` haircut 경로에서도 haircut 이후 수량으로 다시 계산한 RR이 저장되도록 수정
- audit / position signal trace에도 post-size snapshot이 남도록 보강

## 3. latest rerun status

### fresh run
- 시작: `2026-03-30 22:22:46 UTC`
- 최신 확인 시점: `2026-03-30 22:43:46 UTC`

### 현재까지 관측
| 항목 | 건수 |
|---|---:|
| `Signal: BUY` | 0 |
| `Signal filtered` | 0 |
| `Pre-gate execution reject` | 0 |
| `Execution viability compare` | 0 |
| `Trade opened` | 0 |

### 의미
- 이 fresh run은 아직 warmup / watchlist churn 구간이다.
- 따라서 새 telemetry가 live에서 어떻게 찍히는지 아직 판단할 수 없다.
- 지금 시점의 직접 문제는 `poor_execution_viability`가 아니라, fresh run에서 BUY 시그널 자체가 아직 없다는 점이다.

## 4. secondary noise

### 지속되는 항목
- Gecko `429`
- `unsupported_pool_program`
- scanner watchlist churn

### 중요도 해석
- 이 항목들은 운영 노이즈이지만, 최신 21분 구간의 직접적인 0-entry 원인은 아니다.
- 직접 원인은 simply "BUY 표본 부재"다.

## 5. Current Next Step

1. live canary를 더 지속한다.
2. 첫 BUY 시그널이 나오면 `execution.preGate`와 `execution.postSize`를 우선 확인한다.
3. 그 뒤에야 아래를 다시 판단한다.
   - `roundTripCost`가 과대한가
   - `TP2/SL` 구조가 원래 낮은가
   - blacklist pair가 cadence를 다시 죽이는가

## 6. Current Judgment

```text
인프라: 정상
기준 12.2h canary: 0 entry, poor_execution_viability 지배
telemetry patch: 완료
latest fresh run: 아직 BUY 시그널 0건

결론:
  현재 단계의 핵심은 새 telemetry가 실제 BUY 시그널에서 찍히는지
  확인할 표본을 다시 확보하는 것이다.
```
