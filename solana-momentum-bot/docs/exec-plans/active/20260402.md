# 2026-04-02 개선 계획: Signal 밀도 확보

> Status: active
> Updated: 2026-04-02 (3차 점검 — VPS 배포 검증 + MomentumTrigger 진단 추가)
> Context: live 모드 전환 성공, Meteora 배포 확인, 그러나 signal 여전히 0
> Principle: 봇을 더 똑똑하게 만드는 것보다, 더 많이 보게 만드는 게 먼저다.

---

## 현재 병목 (코드 + 운영 로그 검증 완료)

| # | 병목 | 증거 | 영향 | 해결 유형 | 상태 |
|---|------|------|------|-----------|------|
| 0 | **Live→Paper Fallback** | current-session.json = paper. 3가지 preflight 실패 | **live 거래 자체가 불가** | `.env` 변경 | ✅ 해결 (`PREFLIGHT_ENFORCE_GATE=false`, live 전환 확인) |
| 1 | **EXECUTION_RR_BASIS=tp1 (구버전)** | NoKings Grade A signal: effectiveRR=0.71(tp1) vs 8.40(tp2) | 완벽한 시그널도 reject | `.env` 추가 | ✅ 적용 (`tp2`) |
| 2 | **Age bucket 15min floor** | 밈코인 모멘텀 1~2봉(5~10분)이면 끝남 | 최적 진입 구간 전체 차단 | `.env` 변경 | ✅ 적용 (`5min`) |
| 3 | **Meteora reject — VPS 미배포** | 516/994 reject = `unsupported_dex` | 시장 절반 미추적 | 코드 배포 | ✅ 배포 완료 (최근 3h: unsupported_dex=0) |
| 4 | **MAX_CONCURRENT_POSITIONS=1** | config.ts 기본값 1, Bootstrap equity <5 SOL | 하루 4~8건 한계 | `.env` 변경 | ⚠️ `POSITIONS=2` 적용, **`ABSOLUTE=1` 미수정** |
| 5 | **MomentumTrigger silent rejection** | 100K+ pm2 로그에 eval 0줄. reject stats 미출력 | 병목 위치 파악 불가 | 코드 수정 | 🔧 진단 로그 추가 (배포 필요) |
| 6 | **non_sol_quote 급증** | 최근 3h: 987건 reject (100%). Meteora 풀 대부분 USDC/USDT quote | Meteora 복원 효과 제한적 | 구조적 한계 | 📋 인지 (SOL quote 설계 유지) |
| 7 | **Queue overflow** | `dropped=12,483`, limit=250 | 후보 발견해도 처리 누락 | 큐 확대 검토 | 📋 관찰 |

---

## 3차 점검 (4/2 11:30 KST) — MomentumTrigger 진단

### VPS 배포 검증 결과

| 항목 | 결과 |
|------|------|
| Meteora `unsupported_dex` in pre_watchlist_reject | **0건** (최근 3h) — 배포 확인 |
| Live 모드 | **활성** — `PREFLIGHT_ENFORCE_GATE=false` 작동 |
| non_sol_quote | 987건/3h — Meteora 풀 대부분 USDC quote, SOL pair만 거래하는 설계상 정상 |
| Signal | **0** — 여전히 미발생 |

### MomentumTrigger 블랙홀 발견

pm2 로그 100,300줄에서 eval/trigger/signal 관련 로그 = **0줄**.
원인: `momentumTrigger.ts`가 모든 rejection을 silent null로 반환, 로그 미출력.

```
onCandle() 호출 (매 10초 캔들)
  → insufficientCandles? → silent null
  → volumeInsufficient? → silent null
  → noBreakout?         → silent null
  → confirmFail?        → silent null
  → cooldown?           → silent null
  → 전부 통과 시에만 signal 반환 (한 번도 발생 안 함)
```

### 수정 사항

- `momentumTrigger.ts`: `createModuleLogger` 추가, 5분 주기 `[RejectStats]` INFO 로그
- `momentumTrigger.ts`: volume 또는 breakout 하나 이상 통과 시 `Near-miss` DEBUG 로그
- `index.ts`: signal 발생 시 INFO 로그 추가

배포 후 5분마다 아래와 같은 로그가 출력됨:
```
[MomentumTrigger] [RejectStats] evals=342 signals=0 | insuffCandles=1205 volInsuf=340 noBreakout=330 confirmFail=342 cooldown=0
```

이 데이터로 어떤 조건이 가장 큰 병목인지 정량적으로 파악 가능.

---

## Preflight Fallback 분석

### 메커니즘

```
TRADING_MODE=live
  → src/index.ts:249 preflight 실행
  → 실패 시 + PREFLIGHT_ENFORCE_GATE=true
  → effectiveMode='paper' (index.ts:262)
  → 세션 디렉터리: ${timestamp}-paper (persistenceLayout.ts:47)
  → current-session.json에 paper 기록
```

`current-session.json`은 마지막 세션 포인터 — 이전 live 세션이 있어도 이후 paper 세션이 시작되면 덮어씌워진다. `sessions/` 아래의 live 디렉터리들이 그 증거.

### VPS 로그에서 확인된 3가지 실패 원인

| 실패 조건 | 코드 위치 | 로그 메시지 |
|-----------|-----------|------------|
| JUPITER_API_KEY 미설정 | `preflightCheck.ts:147` | "JUPITER_API_KEY missing" |
| 잔고 부족 | `preflightCheck.ts:153` | "balance 0.0100 SOL < 0.0500 SOL" |
| Edge gate | `measurement.ts:104` | "reject_gate (totalTrades<10)" |

### Bootstrap 역설 (Chicken-and-Egg)

```
live 모드 진입 → edge gate 통과 필요 (totalTrades ≥ 10)
trades 쌓기   → live 모드 필요
→ PREFLIGHT_ENFORCE_GATE=true인 한 영원히 paper
```

**해결: Bootstrap 단계에서는 `PREFLIGHT_ENFORCE_GATE=false` 필수.**
이것이 설계 의도 — enforceGate는 Calibration 이후 활성화하는 게이트.

### Preflight 실패 전체 조건 참조

| 검사 항목 | 기본 임계값 | 코드 위치 |
|-----------|------------|-----------|
| 최소 trades | 50 | `preflightCheck.ts:27` |
| 최소 WR | 40% | `preflightCheck.ts:28` |
| 최소 cost-adjusted R:R | 2.0 | `preflightCheck.ts:29` |
| Jupiter API Key | 필수 | `preflightCheck.ts:147` |
| 지갑 잔고 | ≥ 0.05 SOL | `config.ts:231` |
| Edge gate totalTrades | ≥ 10 | `measurement.ts:104` |

---

## 방향 검증: 1 → 100 SOL

### 현재 위치

```
[여기] ─────────────────────────────────────────→ 100 SOL
 1 SOL, 0 trades, signal 0, live fallback to paper
```

2일간 0 trades — 전략이 나빠서가 아니라 파이프라인 3곳이 동시에 막혀있었음.

### 병목 구조

```
시장 노출 → signal 생성 → gate 통과 → live 체결 → edge 검증 → 복리
^^^^^^^^    ^^^^^^^^^^^   ^^^^^^^^^   ^^^^^^^^^
(Meteora)   (age 15min)  (RR tp1)   (preflight)

4개가 동시에 막힘 → 수정하면 처음으로 trade 발생 가능
```

### 전략 자체는 검증됨

- CSV 19-token sweep: TP2=5.0 100% 수렴, SL=1.25 안정
- Micro-replay: MFE/MAE 18.8x (30s), 시그널 품질 확인
- 유일한 Grade A 시그널(NoKings): 실제 MFE +40.9%, gate 설정 오류로 reject

### 배포 후 예상 경로

```
Phase 1 (1~2주): Signal 밀도 확보
  - .env + 코드 배포 → 파이프라인 개통
  - 일 3~5 signals 목표
  - Bootstrap 50 trades 돌파

Phase 2 (3~4주): Edge 확인
  - paper-report expectancy 양수 확인
  - WR 40%+ / PF 1.5+ 검증
  - PREFLIGHT_ENFORCE_GATE=true 복원

Phase 3 (5주~): 복리 구간
  - Kelly 활성화 (Confirmed 50+ trades)
  - 1 → 5 SOL 가속
```

### 리스크

| 리스크 | 심각도 | 대응 |
|--------|--------|------|
| Live edge가 음수일 수 있음 | 높음 | Bootstrap 50 trades 후 expectancy 확인. 음수면 trigger 조정 |
| Age 5min → rug pull 노출 | 중간 | 0.25x sizing tier + security gate 유지 |
| Gecko 429 증가 (watchlist 20) | 낮음 | `SCANNER_DEX_ENRICH_MS` 조정 |
| enforceGate=false 안전망 해제 | 중간 | Bootstrap 1% risk cap, daily 5% loss limit으로 보호 |

---

## 100 SOL 역산 경로

```
1 → 2 SOL   Bootstrap (1% risk, ~70 trades)
2 → 5 SOL   Calibration (1% risk, ~90 trades)
5 → 20 SOL  Confirmed + Kelly 3% (~50 trades)
20 → 100 SOL Proven + Kelly 5% (~35 trades)
             총 ~245 trades, 하루 5건이면 ~7주
```

핵심: Bootstrap 50 trades를 빠르게 돌파해야 복리 구간에 진입한다.
하루 5건 전제 → 10일. 하루 2건이면 25일. **concurrent + signal 밀도가 속도를 결정한다.**

---

## Phase 1: Signal 밀도 확보 (1주차)

### 1A. VPS 최신 코드 배포 (즉시)

- `git pull && npm ci && tsc && pm2 restart` — Meteora whitelist + meteoradbc 반영
- 검증: runtime-diagnostics에서 `unsupported_dex` = "meteora" 0건 확인
- 예상 효과: 후보풀 516/994 = **52% 복원**

### 1B. .env 핵심 수정 (즉시)

```bash
# ─── 치명적 추가 ───
EXECUTION_RR_BASIS=tp2                    # tp1→tp2 (NoKings RR 0.71→8.40)
AGE_BUCKET_HARD_FLOOR_MIN=5              # 15→5분 (밈코인 최적 구간 진입)
PREFLIGHT_ENFORCE_GATE=false              # Bootstrap 역설 해소 — live 진입 허용

# ─── 밀도 확보 ───
MAX_CONCURRENT_POSITIONS=2                # 1→2
MAX_CONCURRENT_ABSOLUTE=3                 # 1→3
MAX_WATCHLIST_SIZE=20                     # 8→20
REALTIME_MAX_SUBSCRIPTIONS=30             # 8→30

# ─── Bootstrap 표준 정상화 ───
MAX_RISK_PER_TRADE=0.01                   # 0.005→0.01 (Bootstrap 1%)
MAX_DAILY_LOSS=0.05                       # 0.02→0.05 (Bootstrap 5%)
MAX_DRAWDOWN_PCT=0.30                     # 0.10→0.30 (Bootstrap 30%)
```

### 1C. 지갑 잔고 확인

- 현재 0.01 SOL → 최소 0.05 SOL 필요 (preflight 통과 조건)
- `LIVE_PREFLIGHT_MIN_WALLET_BALANCE_SOL=0.05` (기본값)
- enforceGate=false로 우회하더라도, 실제 거래에는 SOL 필요

### 1D. Trigger 파라미터 관찰

- 1A~1C 적용 후 24~48h 관찰 (vm=3.0, cb=3, cp=2%)
- signal 0 지속 시 `REALTIME_CONFIRM_MIN_BARS` 3 → 2 완화
- micro-backtest 근거: cb=2가 신호 밀도 2배 이상 (16 vs 34 signals)
- vm=3.0 → 2.5 완화는 1D 관찰 결과에 따라 결정

---

## Phase 2: Edge 확인 (2~3주차)

### 2A. Bootstrap 50 trades 돌파

- paper-report로 expectancy 확인
- 양수 → Calibration 진입
- 음수 → trigger 조건 또는 exit 구조 조정

### 2B. 크론 백테스트 배포

- `scripts/cron-backtest.sh` VPS 배포
- 6시간마다 파라미터셋 A/B/C/D 자동 비교
- Edge Score 추이 Telegram 알림

### 2C. Preflight Gate 복원

- 10+ trades 달성 후 `PREFLIGHT_ENFORCE_GATE=true` 복원
- edge gate 통과 확인 후 live 모드 정상 운영

---

## Phase 3: 복리 구간 (4주차~)

- Confirmed 진입 → Kelly 활성화
- watchlist 안정화 확인 후 추가 확대 가능
- Strategy D (Jito 필요) / Runner Extension 검토

---

## 하지 말 것

- signal 0인 상태에서 파라미터 재스윕 반복
- 표본 없이 전략 구조 변경
- 배포 전에 watchlist만 확대 (Meteora reject 상태에서 빈 슬롯 증가)
- Bootstrap 미탈출 상태에서 Kelly/공격적 사이징 시도
- Age bucket을 0으로 설정 (rug pull 위험 — 최소 5분 유지)
- PREFLIGHT_ENFORCE_GATE=false를 Calibration 이후까지 유지

---

## VPS 배포 체크리스트

```bash
# 1. .env 수정
vi ~/.env  # 위의 1B 항목 반영

# 2. 코드 배포
cd /root/Solana/Solana-Trading/solana-momentum-bot
git pull origin main && npm ci && npx tsc --noEmit

# 3. 지갑 잔고 확인
# SOL balance ≥ 0.05 확인 (거래 실행에 필요)

# 4. 재시작
pm2 restart momentum-bot

# 5. 검증 (24h 후)
# - runtime-diagnostics: unsupported_dex "meteora" = 0
# - current-session.json: tradingMode = "live"
# - signal count > 0
```

---

## 완료 기준

- [x] VPS 최신 코드 배포 → Meteora reject 0건 ✅ (3차 점검 확인)
- [x] EXECUTION_RR_BASIS=tp2 적용 ✅
- [x] PREFLIGHT_ENFORCE_GATE=false → live 모드 진입 확인 ✅
- [x] Age bucket 5min 적용 ✅
- [x] MAX_CONCURRENT_POSITIONS=2 적용 ✅
- [ ] **MAX_CONCURRENT_ABSOLUTE=3 적용** ← 현재 1, 미수정
- [ ] **MomentumTrigger 진단 로그 배포** ← 코드 수정 완료, VPS 배포 필요
- [ ] watchlist 20개 운영 안정
- [ ] RejectStats로 병목 조건 파악 → 필요시 cb=2 완화
- [ ] 일평균 signal ≥ 3
- [ ] Bootstrap 50 trades 돌파
- [ ] paper expectancy 양수 확인
- [ ] PREFLIGHT_ENFORCE_GATE=true 복원 (10+ trades 후)
