Status: current
Updated: 2026-04-07
Purpose: 2026-04-07 live ops loop 이력
Use with: `docs/runbooks/live-ops-loop.md`, `CRITICAL_LIVE.md`

# Ops History - 2026-04-07

## Entry 01

### Meta

- started_at_utc: `2026-04-07T02:26:20.474Z`
- ended_at_utc: `2026-04-07T03:10:00Z`
- operator: `igyubin`
- trigger: `critical live incident review`

### Window

- runtime_window_start_utc: `2026-04-07T00:31:03Z`
- runtime_window_end_utc: `2026-04-07T02:31:03Z`
- runtime_window_hours: `2`
- db_window_start_utc: `2026-04-06T14:31:03Z`
- db_window_end_utc: `2026-04-07T02:31:03Z`
- db_window_hours: `12`
- metric_scope_note: `_recent_2h suffix는 runtime_window 기준(ops:check 기본), _recent_12h suffix는 db_window 기준(trade-report 기본). 두 window가 서로 다른 이유는 ops:check가 2h short window를 권장하고 trade-report는 12h long window를 기본으로 두기 때문이며 incident drill-down 시 일치시키지 않는다`

### Evidence

- synced_at_utc: `2026-04-07T02:26:20.474Z`
- runtime_files:
  - `data/realtime/current-session.json`
  - `data/realtime/runtime-diagnostics.json`
  - `data/realtime/sessions/2026-04-06T14-17-04-255Z-live/realtime-signals.jsonl`
  - `CRITICAL_LIVE.md`
- runtime_commands:
  - `./scripts/sync-vps-data.sh`
  - `npm run ops:check -- --hours 2 --top 10`
  - `npm run ops:check:sparse -- --hours 2 --top 5`
- db_commands:
  - `npx ts-node scripts/trade-report.ts --hours 12`
  - `npm run ops:check:ledger -- --hours 12`
- codex_request:
  - `vps 운영 로그 동기화했어, 최근 2시간 로그 분석을 부탁해`
  - `답변 내용 품질 점검해주고 리포트 출력해줘`

### DB Anomalies

- opened_rows_recent_12h: `8`
- partial_close_rows_recent_12h: `6`
- tp_negative_pnl_rows_recent_12h: `5`
- avg_entry_gap_pct_recent_12h: `-77.39%`
- avg_exit_gap_pct_recent_12h: `-77.59%`
- max_abs_entry_gap_pct_recent_12h: `100.00%`
- max_abs_exit_gap_pct_recent_12h: `100.00%`
- avg_entry_slippage_bps_recent_12h: `1665.5`
- avg_exit_slippage_bps_recent_12h: `18.8`
- tp2_realized_outcome_recent_12h: `0W / 5L | net=-0.008481 SOL`

### Metrics

- signals_recent_2h: `10`
- executed_live_recent_2h: `0`
- risk_rejected_recent_2h: `8`
- gate_rejected_recent_2h: `1`
- execution_failed_recent_2h: `1`
- realized_recent_12h: `-0.008526 SOL`
- opened_rows_recent_12h: `8`
- closed_rows_recent_12h: `6`
- closed_wins_recent_12h: `0`
- closed_losses_recent_12h: `6`
- partial_close_rows_recent_12h: `6`
- tp_negative_pnl_rows_recent_12h: `5`
- avg_entry_gap_pct_recent_12h: `-77.39%`
- avg_exit_gap_pct_recent_12h: `-77.59%`

### Assessment

- quality_check_of_recent_answers: `retroactive: pass — 후속 Phase A/B/C1 배포(Entry 02 deployment context 참조)로 가설 확정. 가격 축 오염 추정 → 실제 entry alignment guard로 제거 가능했음`
- primary_bottleneck: `risk`
- secondary_bottleneck: `incident`
- summary:
  - 최근 운영 구간은 `signal 부족`보다 `risk/gate 차단`과 `429 execution failure`가 직접 blocker로 보임
  - 동시에 `trade-report` 기준 `planned/decision`과 `fill` 가격 축 불일치 의심이 강해 ledger 품질 신뢰도가 낮음
  - 따라서 `risk 차단이 존재한다`와 `risk 차단이 옳다`는 분리해서 해석해야 함

### Action

- taken:
  - `CRITICAL_LIVE.md` 작성 및 최신 근거 고정
  - `tradeExecution` 가격 축 추적을 다음 우선순위로 상향
- deferred:
  - `edge_blacklist` 완화 보류
  - `cooldown` 완화 보류
- forbidden:
  - 가격 정합성 검증 전 ledger 기반 전략 품질 확정 금지

### Next Check

- next_window_utc: `2026-04-07T03:00:00Z ~ 2026-04-07T05:00:00Z`
- expected_change:
  - `tradeExecution` 값 생성 경로별 단위 정합성 확인
  - `TP인데 손실` 사례 재현 및 축 일치 여부 확인
- fallback_if_not_improved:
  - notifier / trade row / execution summary 경로별 raw evidence 추가 수집

## Entry 02

### Meta

- started_at_utc: `2026-04-07T10:20:00Z`
- ended_at_utc: `2026-04-07T11:12:10Z`
- operator: `igyubin`
- trigger: `7h live log re-analysis + long-term improvement plan`
- post_guard_session: `true`
- bot_restart_at_utc: `2026-04-07T03:53:05Z` (`current-session.json` session start)
- session_context: `Entry 01 종료(03:10Z) → Phase A/B/C1 배포 → bot restart(03:53Z) → Entry 02 measurement window(04:01Z~). 즉 이 entry의 모든 metric은 post-guard session 데이터이고, Entry 01의 -77% gap 대비 -0.30%로의 개선은 deployment effect로 해석한다`

### Window

- runtime_window_start_utc: `2026-04-07T04:01:48Z`
- runtime_window_end_utc: `2026-04-07T10:56:09.803Z`
- runtime_window_hours: `7`
- db_window_start_utc: `2026-04-07T04:01:48Z`
- db_window_end_utc: `2026-04-07T11:01:48Z`
- db_window_hours: `7`

### Evidence

- synced_at_utc:
  - `data/realtime/current-session.json` session start = `2026-04-07T03:53:05.856Z`
  - `npm run ops:check -- --hours 7 --top 10` latest data = `2026-04-07T10:56:09.803Z`
  - `data/realtime/runtime-diagnostics.json updatedAt = 2026-04-07T10:56:30.891Z`
- runtime_files:
  - `data/realtime/current-session.json`
  - `data/realtime/runtime-diagnostics.json`
  - `data/realtime/sessions/2026-04-07T03-53-05-856Z-live/realtime-signals.jsonl`
  - `docs/runbooks/live-ops-loop.md`
  - `STRATEGY.md`
  - `OPERATIONS.md`
  - `docs/exec-plans/active/1sol-to-100sol.md`
  - `CRITICAL_LIVE.md` (§7E pre-guard cleanup procedure는 execution/report quality 의존 항목)
- runtime_commands:
  - `npm run ops:check -- --hours 7 --top 10`
  - `npm run ops:check:sparse -- --hours 7 --top 10`
  - `node` one-off parse of `realtime-signals.jsonl` for status / pair / reason mix
  - `node` one-off parse of `runtime-diagnostics.json` for 7h event mix
- db_commands:
  - `results/vps-analysis/trade-report-latest.txt` read for latest synced 4h snapshot only
  - `results/vps-analysis/realized-replay-ratio-latest.md` read for realized vs replay snapshot only
  - user-provided manual `TRADE REPORT (최근 7h)` text used as same-window DB evidence
  - `npx ts-node scripts/trade-report.ts --hours 7` attempted locally but blocked by missing `DATABASE_URL`
- codex_request:
  - `live-ops-loop.md 에 따라 최신 운영 로그 분석을 부탁해, vps 데이터는 동기화 되어있어`
  - `최근 7시간 기준으로 봐주면 좋을 것 같아`
  - `최근 7시간 로그를 다시 한 번 분석해서 1. 어떤 문제가 있는지, 2. 어떤 개선점이 필요할지 등을 요약해줘`
  - `DEX 트레이딩은 저시총 티커에 시총 대비 거래량이 급증할 때 엣지를 취하는게 핵심이잖아`

### DB Anomalies

- realized_recent_7h: `-0.000509 SOL`
- closed_rows_recent_7h: `4`
- partial_close_rows_recent_7h: `2`
- closed_wins_recent_7h: `1`
- closed_losses_recent_7h: `3`
- traded_tokens_recent_7h: `1`
- tp_negative_pnl_rows_recent_7h: `1`
- avg_exit_gap_pct_recent_7h: `-0.30%`
- max_abs_exit_gap_pct_recent_7h: `0.58%`
- avg_exit_slippage_bps_recent_7h: `2500.5`
- fake_fill_rows_recent_7h: `1 / 4` (1건이 `>=9000bps` saturated, 평균 2500.5는 outlier 효과로 추정 — `docs/audits/exit-slip-gap-divergence-2026-04-07.md` 가설 A)
- round_trip_cost_pct_recent_7h: `0.00%`
- note:
  - `TAKE_PROFIT_1`인데 음수 실현 손익 row 존재
  - `decision=fill`, `exitGap=0.00%`인데 `exit_slip=10000bps`가 동시에 기록됨
  - ratio report는 `predicted adj ~= 0` 구간이라 headline ratio 폭주

### Metrics

- runtime_signal_rows_recent_7h: `8`
- trigger_signals_recent_7h: `9`
- executed_live_recent_7h: `2`
- risk_rejected_recent_7h: `4`
- gate_rejected_recent_7h: `1`
- execution_failed_recent_7h: `1`
- alias_miss_recent_7h: `0`
- sparseInsuf_recent_7h: `0`
- pre_watchlist_reject_recent_7h: `39`
- pre_watchlist_reject_non_sol_quote_recent_7h: `39`
- admission_skip_recent_7h: `58`
- unsupported_dex_recent_7h: `45`
- no_pairs_recent_7h: `13`
- candidate_seen_recent_7h: `246`
- candidate_evicted_recent_7h: `253`
- idle_evicted_recent_7h: `252`
- idle_skip_delta_recent_7h: `12286`
- unique_signaled_tickers_recent_7h: `3`
- top_signal_pair_recent_7h: `PIPPIN=6/8 signals`
- realized_recent_7h: `-0.000509 SOL`
- closed_rows_recent_7h: `4`
- tp_negative_pnl_recent_7h: `1`
- metrics_note:
  - `runtime_signal_rows_recent_7h=8`는 `realtime-signals.jsonl` row 기준
  - `trigger_signals_recent_7h=9`는 `ops:check:sparse` trigger stats 기준
  - 두 숫자는 정의가 다르므로 같이 남긴다
  - `candidate_seen 246 < candidate_evicted 253`는 산술 모순처럼 보이지만 두 카운터의 집계 방식이 다르다. `candidate_seen`은 `distinctTokenSet`(`src/reporting/runtimeDiagnosticsTracker.ts:222`)이라 token 단위 dedupe된 set size이고, `candidate_evicted`는 `summarizeTokenEventCount`(:240, :511)로 evict 이벤트 raw count이다. 동일 token이 evicted → readded → evicted로 여러 번 카운트되거나, 윈도 시작 전에 seen된 token이 윈도 안에서 evicted되면 evicted가 seen을 초과할 수 있다 — 정상 동작

### Assessment

- quality_check_of_recent_answers: `pass with refinement`
- primary_bottleneck: `risk`
- secondary_bottleneck: `execution`
- tertiary_bottleneck: `universe`
- summary:
  - 최근 두 답변의 핵심 판단은 유지한다. 최근 7h의 본질은 `signal scarcity`가 아니라 `PIPPIN 집중 -> 1W/3L 손실 -> cooldown 차단`이다.
  - 다만 전략 개선 논의에는 `찾는 풀의 품질`과 `국소적 ticker concentration`을 더 직접 넣어야 한다. low-cap / volume-mcap edge 관점이 빠지면 운영 개선이 단순 튜닝으로 흐를 수 있다.
  - runtime 근거상 `alias / overflow / sparse`는 주병목이 아니고, `risk_rejected 4`, `execution_failed 1`, `candidate turnover 246 seen / 253 evicted / 252 idle`가 실제 병목이다.
  - DB 근거상 `1W / 3L`, `-0.000509 SOL`, `TAKE_PROFIT_1 but negative PnL`, `exit_slip 10000bps`는 리포트/실행 품질과 수익성 구조를 동시에 의심하게 만든다.

### Problem Summary

- confirmed_from_logs:
  - `PIPPIN` 1종목 집중: 최근 7h 신호 8건 중 6건이 동일 ticker
  - 진입 2건 뒤 후속 신호 4건이 `portfolio cooldown` / `per-token cooldown`으로 차단
  - 실현 성과는 `1W / 3L`, `-0.000509 SOL`
  - `non_sol_quote`, `unsupported_dex`, `idle eviction` 비중이 높아 탐색 breadth 대비 tradable breadth가 좁음
  - `4ytp...`는 low-cap / high volume-mcap 쪽 후보였지만 runtime status는 `execution_failed`, cause는 `Phase A3 entry alignment guard`(`src/orchestration/tradeExecution.ts` `assertEntryAlignmentSafe`)에 의한 차단. 즉 fill 단위 폭발 의심으로 인한 의도된 차단이며, runtime label만 보면 단순 실패로 오인 가능
- implication:
  - 현재 운영은 `좋은 low-cap surge를 더 많이 포착`하기보다 `좁은 universe 안에서 특정 ticker 재노출` 쪽으로 기울어 있다
  - 따라서 손실과 국소화 문제는 단순 risk/cooldown 조정이 아니라 `pool discovery quality + concentration control + edge cohort measurement` 문제다

### Action

- taken:
  - 최근 두 답변의 품질을 재검토하고, 장기 개선 방향을 ops-history에 고정
  - `DEX edge = low-cap / volume-mcap spike / executable liquidity / freshness` 관점을 현재 운영 병목 해석에 연결
  - `avg_exit_slippage_bps_recent_7h=2500.5`(=25%)와 `avg_exit_gap_pct_recent_7h=-0.30%`의 산술적 불일치를 P1 조사 항목으로 escalation. DB anomalies note에만 있던 이슈를 Action으로 격상. 조사 범위는 `closeTrade` exit 가격 산출 경로(`src/orchestration/tradeExecution.ts`)와 `trade-report` slippage 집계 경로(`scripts/trade-report.ts`) 양쪽 — 두 경로가 같은 reference price / sign convention / bps unit을 쓰는지 비교. 산출물은 `docs/audits/exit-slip-gap-divergence-2026-04-07.md`로 고정.
- deferred:
  - risk cooldown 완화
  - bootstrap 파라미터 추가 완화
  - unsupported_dex breadth 확장
- forbidden:
  - 리포트/실행 품질 불일치가 남아 있는 상태에서 `1W / 3L`만 보고 cooldown 완화로 대응하는 것 — 구체적으로는 `CRITICAL_LIVE.md §7E pre-guard cleanup`이 sanitizer로 자동 격리 중인 pre-guard rows가 EdgeTracker / cooldown 판단을 오염시키지 않는지 audit 통과 전까지 risk loosening 금지. sanitizer는 execution/report 정합성 도구이지 concentration 도구가 아님에 주의
  - stale/idle 문제를 해결하지 않은 채 watchlist만 넓히는 것

### Long-Term Improvement Plan

- exec_plan_link: `docs/exec-plans/active/edge-cohort-quality-2026-04-07.md` (3축의 owner / target_files / acceptance_criteria / lifecycle은 모두 해당 exec-plan에서 관리. 이 ops-history는 진단 기록으로 immutable, exec-plan은 실행 기록으로 lifecycle을 가짐)

- axis_1_universe_quality:
  - 목표: `더 많은 pool`이 아니라 `더 맞는 pool` 확보
  - 근거: `candidate_seen 246` 대비 `candidate_evicted 253`, `idle_evicted 252`, `non_sol_quote 39`, `unsupported_dex 45`
  - 방향:
    - stale/idle eviction 계속 강화
    - admission 단계에서 `SOL quote`, `supported dex`, `min TVL`, `marketCap band`, `token age band`를 더 앞단에서 반영
    - live freshness는 유지하되 breadth 확장보다 executable low-cap universe 정제가 우선

- axis_2_concentration_control:
  - 목표: 진짜 edge 집중과 나쁜 universe가 만든 concentration을 구분하고, 후자를 억제
  - 근거: 최근 7h 신호 `8` 중 `PIPPIN 6`, 실제 진입은 2건 모두 `PIPPIN`, 이후 cooldown 차단 4건
  - 방향:
    - 기존 `SAME_PAIR_OPEN_POSITION_BLOCK`, `PER_TOKEN_LOSS_COOLDOWN_*`는 유지
    - 추가로 `recent signal concentration cap` 또는 `same-ticker shadow demotion` 같은 사전 분산 장치 검토
    - 운영 리뷰에 `top mint concentration`을 상시 지표로 포함

- axis_3_edge_measurement_by_cohort:
  - 목표: bootstrap edge가 어디에서 나오는지 `marketCap / volumeMcap / freshness` 코호트별로 분리
  - 근거: `PIPPIN`은 대형 밈 continuation에 가깝고, `4ytp...`는 저시총 / high volume-mcap edge 가설에 더 가까웠음
  - 방향:
    - signal / trade / realized PnL을 `marketCap cohort`, `volumeMcap cohort`, `freshness cohort`로 분리 집계
    - `volumeMcap`은 현재 boost로만 쓰이므로, 향후 admission or ranking 강화를 검토
    - "우리 edge가 정말 low-cap surge인가"를 replay가 아니라 live/paper cohort 결과로 확인

### Next Check

- next_window_utc: `2026-04-07T11:00:00Z ~ 2026-04-07T18:00:00Z`
- expected_change:
  - `top mint concentration` 재발 여부
  - `candidate_seen 대비 idle_evicted` 비율 완화 여부
  - `PRICE_ANOMALY_BLOCK` 발생 빈도가 정상 fill 분포 대비 normal range인지(즉 guard가 over-fire하는지) 측정 — 단순 재발 여부가 아니라 false-positive rate 관점
  - `risk_rejected`가 보호장치인지 과차단인지 판단 가능한 추가 표본 확보
- fallback_if_not_improved:
  - `pool discovery / admission / idle eviction / signal concentration`을 별도 실행 계획으로 분리
  - `marketCap cohort + volumeMcap cohort` 측정 설계 문서화

## Entry 03

### Meta

- started_at_utc: `2026-04-07T13:32:00Z`
- ended_at_utc: `2026-04-07T13:50:00Z`
- operator: `igyubin`
- trigger: `Phase E P0~P3 deploy verification (live-ops-integrity-2026-04-07.md Phase D + V)`
- post_deploy_session: `true`
- bot_restart_at_utc: `2026-04-07T12:21:19Z` (`notifier-events.jsonl` recovery report)
- session_context: `Entry 02 종료(11:12Z) → Phase E P0~P3 quality fix 추가 정리(13:00Z) → VPS sync(13:32Z) → Entry 03 measurement window. 이 entry의 모든 metric은 12:21Z 재기동 후 1h12min 데이터이고, Phase D acceptance 종결 + Phase V D-immediate 검증 + Phase M day-1 baseline 기록을 함께 한다`

### Window

- runtime_window_start_utc: `2026-04-07T12:21:19Z`
- runtime_window_end_utc: `2026-04-07T13:33:04Z`
- runtime_window_hours: `1.2`
- db_window_start_utc: `2026-04-07T12:21:19Z`
- db_window_end_utc: `2026-04-07T13:33:04Z`
- db_window_hours: `1.2`

### Evidence

- synced_at_utc:
  - `bash scripts/sync-vps-data.sh` snapshot = `data/vps-trades-20260407-223200.jsonl` (135 rows, 162344 bytes)
  - `data/realtime/sessions/2026-04-07T12-21-19-322Z-live/` 전체 동기화
  - `data/realtime/notifier-events.jsonl` 동기화
  - `data/realtime/runtime-diagnostics.json` 동기화
- runtime_files:
  - `data/realtime/sessions/2026-04-07T12-21-19-322Z-live/realtime-signals.jsonl` (1 row)
  - `data/realtime/sessions/2026-04-07T12-21-19-322Z-live/signal-intents.jsonl` (1 row)
  - `data/realtime/notifier-events.jsonl` (Telegram I/O 감사)
  - `data/realtime/runtime-diagnostics.json` (3356 events 누적)
  - `data/vps-trades-latest.jsonl` (135 rows, 17 with exit_slippage_bps set)
- runtime_commands:
  - `jq` ad-hoc parse on trades / runtime-diagnostics / notifier-events
  - `npx ts-node scripts/analysis/realized-replay-ratio.ts --mode live --horizon 180`
- db_commands:
  - 로컬 DATABASE_URL 미설정으로 `trade-report.ts` 직접 실행 보류 — `printSlippageRawAndTrimmed` / `FAKE-FILL WARNING` 로직은 jq 시뮬레이션으로 대체 검증
- codex_request:
  - `배포 후에 vps 운영 로그 동기화 했어, 최근 1시간 로그 분석을 부탁해`
  - `pippin은 기존에 남아있는 포지션 종료였어` (정정)

### Phase D Acceptance Verification

- column_exists: `pass`
  - `vps-trades-latest.jsonl` 135 rows 모두 keys에 `exit_anomaly_reason` 포함
  - `tradeStore.initialize()` ALTER TABLE 자동 마이그레이션 동작 확인
- post_restart_first_close_ok: `pass`
  - `dd2a6b4e-f309-40c0-a43b-a797b309d29e` (pippin)
  - created_at = `2026-04-07T12:17:13Z` (pre-deploy entry)
  - closed_at = `2026-04-07T12:32:18Z` (post-deploy unwind, restart 11min 후)
  - exit_reason = `TIME_STOP`, pnl = `+0.000542 SOL`, exit_slippage_bps = `35`
  - `notifier-events.jsonl` trade_close attempt → result = `ok`
  - 신규 sanitizer 경로 (`resolveExitFillOrFakeFill`, Phase A4 close-time guards, `mergeAnomalyReasons`)가 정상 fill 1건을 깨끗이 통과
- log_rotation_check: `n/a` (post-deploy 1h 동안 `[FAKE_FILL]` prefix 출력 0건)

### Phase V D-immediate Verification (DB UPDATE 없이)

- realized_replay_ratio_anomaly_filter: `pass`
  - stdout: `Anomaly filter (>=9000bps slippage or exit_anomaly_reason set): 1 parent groups (2 rows) excluded`
  - markdown header: `Closed trades: raw=133, clean=131 (anomaly filter excluded 1 parent groups / 2 rows)`
  - parent group 단위 drop이 정확히 `2207984d` (07:50 TP1 partial parent, exit_slip=10000) + `694ca489` (07:52 STOP_LOSS child remainder) 두 row를 함께 제거
  - 기대값 정정: live-ops-integrity-2026-04-07.md Phase V 의 `excluded 1/1`은 실제로는 `1 group / 2 rows` (TP1 partial child가 parent_trade_id로 묶여 있음)
- trade_report_fake_fill_logic: `pass (jq simulation)`
  - 전체 135 rows 중 `exit_slippage_bps != null` n=17
  - raw avg = `598.3 bps` (saturated 1건 outlier)
  - trimmed avg (excluded 1 saturated >=9000bps) = `10.7 bps`
  - FAKE-FILL WARNING 후보 row = `1/135` (`exit_anomaly_reason set OR exit_slippage_bps>=9000`)
  - 즉 trade-report.ts가 prod DB 환경에서 돌면 `printSlippageRawAndTrimmed`와 `FAKE-FILL WARNING` 두 섹션이 정확히 위 값을 출력할 것
- per_row_anomaly_subline: `deferred`
  - `trade-report.ts:274-276`의 per-row 서브라인 `anomaly=...` 표시는 `trade.exit_anomaly_reason`가 truthy일 때만 출력됨
  - 04-07T03:53Z 세션 4건은 모두 pre-deploy close라 column이 NULL → 자연 검증 불가
  - 다음 자연 발생 fake-fill 또는 Phase A4 marker fire 시점까지 검증 보류 (option A: `UPDATE trades SET exit_anomaly_reason='fake_fill_no_received(closeTrade),slippage_saturated=10000bps' WHERE id='2207984d-...'` 1회 SQL backfill 으로 즉시 검증 가능하지만 prod DB 직접 write가 부담스러워 보류)

### Activity Metrics (post-deploy 1h12min)

- runtime_signal_rows_recent_1h: `1` (`13:26:40Z` pippin, breakoutGrade A, gate.passed=true → processing.status=risk_rejected)
- new_entries_recent_1h: `0`
- closed_rows_recent_1h: `1` (dd2a6b4e, pre-deploy entry의 자연 unwind)
- realized_recent_1h: `+0.000542 SOL`
- realtime_candidate_seen_recent_1h: `62` (distinct token = `39`)
- candidate_evicted_recent_1h: `54` (`idle` 100%)
- admission_skip_recent_1h: `29` (`unsupported_dex` 21 + `no_pairs` 8)
- pre_watchlist_reject_recent_1h: `5` (`non_sol_quote`)
- risk_rejection_recent_1h: `1` (`token_safety` — `Dfh5DzRgSvvCFDoYc2ciTkMrbDfRKybA4SoFbPmApump` Token too new: 8min < 15min hard floor)
- trigger_stats_snapshots_recent_1h: `16` (12:24~12:39 pre-restart 잔존 카운터 → 12:42 reset → 12:42~13:21 대부분 `evals=0` → 13:27 `evals=6 signals=1 activePairs=1` 1회만 활성)
- exit_anomaly_reason_set_recent_1h: `0 / 1` (post-deploy 1건 close는 clean)
- fake_fill_rows_recent_1h: `0 / 1` (`>=9000bps` 또는 marker set 기준)
- anomaly_filter_excluded_recent_1h: `0 parent groups / 0 rows` (realized-replay-ratio.ts F1-deep-5 기준, 1h 윈도)

### Notifier I/O Audit (recovery → heartbeat)

- 12:01:03Z signal pippin BUY → 12:01:06Z price_anomaly + raw trade_execution Transient error (pre-deploy)
- 12:12:11Z signal pippin BUY → 12:12:14Z price_anomaly + alert:critical bootstra Error (pre-deploy)
- 12:17:11Z signal pippin BUY → 12:17:13Z trade_open `dd2a6b4e` ok (pre-deploy entry)
- 12:21:19Z recovery `Position e1e93e40-...: MO` ← **deployment moment**
- 12:22:20Z info:lifecycle `Bot started v0.5 — Phase 2 Core Live, mode: live`
- 12:32:18Z trade_close `dd2a6b4e` TIME_STOP ok ← **post-deploy first closeTrade**
- 13:00:21Z info:heartbeat `4h 잔액 1.3097 SOL | 손익 +0.0005 SOL 최근 4h 진입 1건`
- 13:31:10Z raw heartbeat (no change)
- Telegram I/O fail count: `0`
- notifier-events.jsonl append 동작 확인 (Phase C1 Notifier 감사)

### Assessment

- quality_check_of_recent_answers: `corrected: 사용자 정정으로 dd2a6b4e를 lingering position 자연 unwind로 reframe. TD-2 loser pair 재유입 framing은 1h 표본 부족으로 보류.`
- primary_bottleneck: `none (deploy verification window)`
- secondary_bottleneck: `universe` (post-deploy 1h12min 동안 신규 entry 0건, candidate diversity 39 distinct token이지만 100% idle eviction)
- summary:
  - Phase D 두 acceptance 모두 통과. `exit_anomaly_reason` column 존재 + dd2a6b4e clean unwind.
  - Phase V D-immediate 두 항목 통과. realized-replay-ratio 헤드라인과 trade-report 시뮬레이션 모두 fake-fill 1건을 정확히 격리.
  - Phase V D-deferred 1건 (per-row `anomaly=` 서브라인) 은 historical row가 NULL이라 자연 검증 불가, 다음 자연 발생까지 보류.
  - Activity metrics는 deploy verification 외엔 의미 있는 신호 없음 — 1h 표본으로 risk/concentration 판단 금지.
  - Loser pair concentration framing(`dd2a6b4e = 신규 시도`)은 정정됨. 13:26Z pippin 신호는 리체크 아닌 신규 시도였고 hard floor에 정상 차단.

### Action

- taken:
  - Phase D 두 체크박스 종결 (live-ops-integrity-2026-04-07.md)
  - Phase V 체크박스 중 F1-deep-5 stdout/header 두 항목 종결, 기대값 `excluded 1/1` → `excluded 1 group / 2 rows` 정정
  - Phase V D-deferred 1건 (per-row `anomaly=` 서브라인) 은 별도 deferred 항목으로 분리
  - ops-history Entry 03 작성으로 Phase M day-1 baseline 확보
- deferred:
  - per-row `anomaly=` 서브라인 검증 (다음 자연 발생까지)
  - Phase M 7일 모니터링 본격 가동 (day-2부터 분포 누적)
  - prod DB historical row UPDATE 1회 backfill (검증 가치 < write 부담)
- forbidden:
  - 1h 표본만으로 universe quality / concentration / loser pair framing 결론 도출 금지
  - Phase D/V 통과를 근거로 `BOT_BYPASS_EDGE_BLACKLIST` 같은 backdoor 활성화 금지

### Next Check

- next_window_utc: `2026-04-07T18:00:00Z ~ 2026-04-08T13:00:00Z` (~19h, day-1→day-2 경계)
- expected_change:
  - 신규 entry 1건 이상 발생 (현재 0)
  - 신규 close에서 `exit_anomaly_reason` 자연 발생 여부
  - candidate_seen / candidate_evicted 비율이 7h 윈도(Entry 02 246/253) 대비 변화
  - PIPPIN 외 ticker가 candidate set에 진입하는지 (Entry 02 `top_signal_pair=PIPPIN=6/8` 패턴 재발 여부)
- fallback_if_not_improved:
  - 24h 내 entry 0건 지속 시 `admission_skip:unsupported_dex=21` 비중 분석 (어떤 DEX들이 차단되는지)
  - Phase V per-row 서브라인 검증을 위해 prod DB UPDATE 1회 backfill 재고려

## Entry 04

### Meta

- started_at_utc: `2026-04-07T14:18:00Z`
- ended_at_utc: `2026-04-07T14:30:00Z`
- operator: `igyubin` + `claude_local` (ralph-loop iter7)
- trigger: `signal-cohort audit — 사용자 가설 (저시총 고거래량 edge) 정량 검증`
- post_deploy_session: `true`
- session_context: `Entry 03 종결 후 ralph-loop iter7. 'Long-Term Improvement Plan' axis_3 acceptance 첫 칸을 read-only로 부분 충족하기 위해 scripts/analysis/signal-cohort-audit.ts 신설. trades 테이블에 marketCap 컬럼이 없어 signal-intents.jsonl(Feature 2 이후 모든 세션)을 입력으로 사용. 이 entry는 trades window 측정이 아니라 signal-level cohort 분포 + 가설 검증 기록`

### Window

- runtime_window_start_utc: `2026-04-06T03:20:29Z`
- runtime_window_end_utc: `2026-04-07T13:33:04Z`
- runtime_window_hours: `34.2`
- db_window: `n/a` (signal-intents jsonl만 사용, DB 미접근)
- meta_note: signal-intents.jsonl은 04-05 commit 076e1f4 이후 생성. 4 sessions 86 signals 71 with marketCapUsd

### Evidence

- runtime_files:
  - `data/realtime/sessions/2026-04-06T03-20-29-395Z-live/signal-intents.jsonl` (33 rows, 18 with mc)
  - `data/realtime/sessions/2026-04-06T14-17-04-255Z-live/signal-intents.jsonl` (38 rows, 38 with mc)
  - `data/realtime/sessions/2026-04-07T03-53-05-856Z-live/signal-intents.jsonl` (14 rows, 14 with mc)
  - `data/realtime/sessions/2026-04-07T12-21-19-322Z-live/signal-intents.jsonl` (1 row, 1 with mc)
- runtime_commands:
  - `npx ts-node scripts/analysis/signal-cohort-audit.ts --out docs/audits/signal-cohort-2026-04-07.md`
- generated_artifact:
  - `docs/audits/signal-cohort-2026-04-07.md` (마크다운 cohort cross-tab + per-signal detail)

### Cohort Verdict

| Cohort | Definition | Signals | Executed | Exec Rate |
|---|---|---:|---:|---:|
| **low-cap surge** | mc<$1M AND ratio>1.0 | 24 | 7 | **29.2%** |
| **high-cap continuation** | mc≥$10M AND ratio<0.5 | 43 | 7 | **16.3%** |

### Hypothesis Status (사용자 가설 — "저시총 고거래량 edge")

- **partial confirm (통과율)**: low-cap surge cohort exec rate 29.2%가 high-cap continuation 16.3%보다 약 1.8배 높다. 즉 universe → execution path 측면에서는 저시총 surge가 더 잘 통과한다.
- **partial reject (실측 손익)**: low-cap surge에서 실제 진입한 7건 (LLM × 2, stonks × 3, BTW × 2)이 **모두 손실**로 끝났음이 cooldown reason으로 확인됨 (`Cooldown active: 6/7/9/10 consecutive losses`, `Per-token cooldown 2 recent losses`). 단, unique token = 3개라 cohort-level statistical significance는 없다.
- **inconclusive (극단 저시총)**: 가장 극단 저시총 후보(`4ytp...pump` mc=$44K, ratio=3.48)는 0건 진입. 04-07 03-53 session에서 `[PRICE_ANOMALY_BLOCK] Entry ratio 0.000000 outside [0.7, 1.3]`로 차단. Phase A3 `assertEntryAlignmentSafe`가 fill 단위 폭발을 의도적으로 막은 것이지만, 이로 인해 가설 검증 표본은 0이다.
- **Phase A3 false positive 의심**: 04-07 03-53 session BTW × 2건도 동일 PRICE_ANOMALY_BLOCK으로 차단. 그러나 BTW는 04-06 14-17 session에서 이미 2건 executed_live → 손실 → cooldown 진입한 historical loser. 즉 BTW의 04-07 차단은 가드의 의도적 격리이지 false positive로 단정할 수 없다.

### 차단 사유 분포 (low-cap surge cohort, 17건)

- `Cooldown active (consecutive losses)`: **9건** (LLM, stonks, BTW의 risk_rejected)
- `Per-token cooldown (2 recent losses)`: **3건**
- `Quote error (Jupiter API)`: **2건** (gate_rejected, getaddrinfo ENOTFOUND)
- `[PRICE_ANOMALY_BLOCK] Entry ratio 0.000000`: **3건** (4ytp + BTW × 2)
- `Swap failed after 3 attempts`: **1건** (`49` token, $631K)

→ 가드/cooldown 차단 17건 중 12건이 **연속 손실 cooldown**, 3건이 **fill 단위 폭발 가드**, 2건이 **Jupiter API 일시 장애**다. 이전 framing("저시총 후보가 가드에 봉인됨")은 PRICE_ANOMALY_BLOCK 3건에 한해서만 정확하다.

### Assessment

- quality_check_of_recent_answers: `revise: 이전 답변(Entry 03 직후 사용자 응답)에서 'low-cap surge cohort 0% pass rate (3/3 PRICE_ANOMALY_BLOCK)'이라고 단순화. 실제로는 24/7 = 29.2% pass rate이며, 차단 17건 중 PRICE_ANOMALY_BLOCK은 3건뿐이다. cohort 측정 데이터를 trades 테이블 기준으로 잡았는데 marketCap 컬럼이 없어 signal-intents.jsonl로 데이터 소스를 정정해야 했다.`
- primary_finding: `사용자 가설은 통과율 측면에서 partial confirm, 실측 손익 측면에서 partial reject (n=3 unique token, 7 trades all loss). 가장 흥미로운 표본(4ytp 극단 저시총)은 가드 차단으로 0건 → inconclusive.`
- secondary_finding: `cooldown framework이 같은 토큰 반복 시도를 정확히 차단하고 있다. LLM/stonks/BTW의 cooldown reason 카운터(6→7→9→10)가 step-up되는 것을 보면 risk loop이 의도대로 작동.`
- summary:
  - axis_3 (cohort measurement) acceptance 첫 칸을 signal-level에서 부분 충족
  - axis_3 acceptance 둘째 칸 (cohort별 R-multiple ≥30 trades)은 7 trades / 3 unique token으로 한참 못 미침. 7d 누적 후 재측정 필요
  - 7건 모두 손실 패턴은 statistical signal이라기보다 universe noise 가능성이 높다 (n=3)
  - 다음 단계: Phase M 7일 동안 low-cap surge cohort 신규 표본을 monitor + 4ytp 같은 극단 저시총의 PRICE_ANOMALY_BLOCK false positive rate 별도 측정

### Action

- taken:
  - `scripts/analysis/signal-cohort-audit.ts` 신설 (read-only, DB 의존성 0)
  - `docs/audits/signal-cohort-2026-04-07.md` 자동 생성
  - 사용자 가설 verdict 표 + 차단 사유 분포 본 entry에 기록
- deferred:
  - cohort × R-multiple 분리 (axis_3 acceptance 둘째 칸) — 7d 누적 + trades 테이블에 cohort 컬럼 추가 또는 JOIN 로직 필요
  - PRICE_ANOMALY_BLOCK false positive rate 측정 — F1-deep audit과 연동
- forbidden:
  - 7 trades / 3 unique token만으로 사용자 가설 최종 reject 금지 (표본 부족)
  - cohort cross-tab 1회 결과로 가드 임계값 변경 금지 (Phase M 7일 누적 전까지)

### Next Check

- next_window_utc: `2026-04-08T13:00:00Z ~ 2026-04-14T13:00:00Z` (Phase M 7d 누적)
- expected_change:
  - low-cap surge cohort signals 24 → ?건 추가
  - 4ytp 또는 유사 극단 저시총 후보의 PRICE_ANOMALY_BLOCK 발화 빈도
  - cooldown 14건 reason 분포의 stability (loss-rate가 표본 증가에 따라 평균회귀하는지)
- fallback_if_not_improved:
  - 7d 후에도 low-cap surge cohort 7 trades / 7 losses 패턴이 유지되면 → 가설 reject 정량 근거 확보, axis_3 acceptance 둘째 칸 부분 reject로 종결
  - 새 unique token이 ≥5개 진입 + win rate 개선되면 → axis_3 둘째 칸 부분 confirm
  - candidate set 좁음이 구조적 문제인지 시간대 변동인지 day-2 대비로 판단
