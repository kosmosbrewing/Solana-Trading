Status: current
Updated: 2026-04-08
Purpose: 2026-04-08 live ops loop 이력 (Phase M day-2)
Use with: `docs/runbooks/live-ops-loop.md`, `CRITICAL_LIVE.md`, `docs/audits/price-anomaly-ratio-2026-04-08.md`

# Ops History - 2026-04-08

## Entry 05

### Meta

- started_at_utc: `2026-04-08T00:30:00Z`
- ended_at_utc: `2026-04-08T01:15:00Z`
- operator: `igyubin` + `claude_local` (ralph-loop iter8)
- trigger: `Codex 운영 로그 분석 → PRICE_ANOMALY 100% 7h window 정밀 진단`
- post_deploy_session: `true`
- bot_restart_at_utc: `2026-04-07T14:35:58Z` (`current-session.json` session start)
- session_context: `Entry 04 종결(14:30Z) → bot restart(14:35Z) → 7h 운영 후 Codex가 PRICE_ANOMALY 15/15 100% rate 발견 → ralph-loop iter8 Phase 1 read-only 진단. 이번 entry는 trades window 측정이 아니라 PRICE_ANOMALY ratio 토큰별 상수 패턴의 root cause 분석`

### Window

- runtime_window_start_utc: `2026-04-07T15:13:02.733Z`
- runtime_window_end_utc: `2026-04-07T22:13:02.733Z`
- runtime_window_hours: `7`
- db_window: `n/a` (signal-intents.jsonl + realtime-signals.jsonl 직접 파싱, DB 미접근)
- session_dataset_dir: `data/realtime/sessions/2026-04-07T14-35-58-100Z-live`

### Evidence

- runtime_files:
  - `data/realtime/current-session.json`
  - `data/realtime/sessions/2026-04-07T14-35-58-100Z-live/realtime-signals.jsonl`
  - `data/realtime/sessions/2026-04-07T14-35-58-100Z-live/signal-intents.jsonl`
  - `data/realtime/runtime-diagnostics.json`
- runtime_commands:
  - `npm run ops:check:sparse -- --hours 7`
  - `node` ad-hoc parse on signal-intents.jsonl PRICE_ANOMALY error message extraction
  - 코드 reading: `src/orchestration/tradeExecution.ts:893-935`, `src/orchestration/signalProcessor.ts:300-466`, `src/executor/executor.ts:200-450`, `src/realtime/microCandleBuilder.ts`, `src/realtime/swapParser.ts:1-387`, `src/realtime/pumpSwapParser.ts`
- generated_artifact:
  - `docs/audits/price-anomaly-ratio-2026-04-08.md` (Phase 1 read-only diagnosis, 4 suspect ranking + decisive verification path)
- codex_request:
  - `최근 운영 로그 분석을 부탁해` (Codex가 28 signals 중 15 PRICE_ANOMALY 발견)

### Status Distribution (7h window)

| Status | Count | Note |
|---|---:|---|
| `execution_failed` | 15 | 100% PRICE_ANOMALY_BLOCK |
| `risk_rejected` | 9 | cooldown 누적 |
| `executed_live` | 4 | pippin × 2, swarms × 2 |
| **Total** | **28** | 100% pippin(14)+swarms(14) |

### Per-Ticker × Status

| ticker | execution_failed | risk_rejected | executed_live |
|---|---:|---:|---:|
| pippin | 7 | 5 | 2 |
| swarms | 8 | 4 | 2 |

### PRICE_ANOMALY Ratio Pattern

| ticker | mc | volMcap | TVL | mean ratio | inflation | σ |
|---|---:|---:|---:|---:|---:|---:|
| pippin | $34.2M | 0.15 | $4.7M | 0.185 | 5.41× | <5% |
| swarms | $14.7M | 0.17 | $1.6M | 0.031 | 32.26× | <8% |

**핵심 관찰** (Phase 1 read-only):

1. ratio가 토큰별로 다르지만 같은 토큰 내에서는 거의 일정 (pippin 0.18~0.19, swarms 0.030~0.032)
2. 같은 토큰에서 30분 내 success/failure interleave — 토큰 영구 unit bug 아님 (path-dependent)
3. inflation 5.41× / 32.26×는 정수 decimals shift(10^k) 아님 — 단순 decimals 누락 아님
4. PRICE_ANOMALY 에러 메시지에 `outputDecimals=6` 정상 출력 → decimals 해결 path는 OK
5. `actualInputUiAmount=0.131` 정상 → executor가 실 SOL 사용량은 정확히 보고

### Code Path Verdict

- **Phase A3 가드는 true positive** (`src/orchestration/tradeExecution.ts:893-935` `assertEntryAlignmentSafe`)
- 차단의 원인은 timing/sandwich가 아니라 **candle.close 산출 path의 오염** (priceNative computation upstream)
- `buildEntryExecutionSummary` (`src/orchestration/signalProcessor.ts:396-466`)는 Phase A2 fix로 `hasActualIn && hasActualOut` 양쪽 set일 때만 실측 사용 — 부분 fallback 가능성 배제

### Suspect Ranking (audit doc 상세)

| Rank | Path | 확률 | 검증 방법 |
|---|---|---:|---|
| 1 | `parsePumpSwapInstruction` offset 16 = `max_sol_cost` 디코딩 | ~70% | PumpSwap IDL discriminator `[102,6,61,18,1,218,235,234]` IDL 확인 (code-only) |
| 2 | `parseFromPoolMetadata` `sumMintDelta` partial delta | ~20% | 1건의 tx pre/post token balances dump |
| 3 | `pickLargestTokenDelta` multi-hop intermediate token | ~5% | pool metadata 존재 여부 확인 |
| 4 | Helius WS feed 자체 inflation | ~5% | `heliusWSIngester.ts` read |

### Assessment

- quality_check_of_recent_answers: `revise: 초기 framing은 'ratio 0.18 globally'였으나, 정밀 측정 결과 pippin=0.18 / swarms=0.03 두 토큰별 상수다. 단일 ratio가 아니라 토큰별 path-dependent inflation. 이 정정으로 root cause 후보가 'global decimals bug'에서 'parser branch별 priceNative 산출 path'로 좁혀졌다`
- primary_finding: `Phase A3 가드는 정확히 동작 중. 5.41×~32.26× inflation은 [0.7, 1.3] 임계로 100% 차단되어 ledger 진입 봉쇄. 가드가 없었다면 4 entry → 최소 4건의 inflation row가 EdgeTracker / cooldown / blacklist 학습을 오염시켰을 것`
- secondary_finding: `차단된 차단 자체는 정상이지만 차단 사유가 발생한다는 사실 자체가 priceNative computation에 path-dependent bug가 있음을 의미. iter9에서 PumpSwap instruction layout 검증 후 1순위 verdict 확정 필요`
- tertiary_finding: `7h window concentration: pippin/swarms 두 ticker가 28 signals를 50/50 분할. axis_2 'top_signal_pair / total ≤ 0.5' acceptance를 우회하는 패턴(top2=1.0). edge-cohort-quality-2026-04-07.md axis_2 caveat에 명시`
- summary:
  - Phase 1 read-only 진단 완료. 코드 변경 없음. 4 suspect ranking 완성.
  - iter9에서 PumpSwap IDL 1단계 code-only verification 진행
  - iter10에서 1순위 확정 시 fix code 작성 + unit test + 배포
  - Phase 2 (4 entries per-trade decomposition)는 별도 iter

### Action

- taken:
  - `docs/audits/price-anomaly-ratio-2026-04-08.md` Phase 1 read-only diagnosis 생성
  - `CRITICAL_LIVE.md` §7H 추가 (iter8 PRICE_ANOMALY 정밀 진단 entry)
  - `docs/exec-plans/active/edge-cohort-quality-2026-04-07.md` Axis 2 acceptance에 7h window caveat 추가 (top2 누적 점유율)
  - 본 ops-history Entry 05 작성으로 day-2 baseline + iter8 결과 고정
- deferred:
  - iter9: PumpSwap IDL discriminator code-only verification (다음 ralph-loop iter)
  - iter10: 1순위 확정 시 `parsePumpSwapInstruction` 폐기 + `parseFromPoolMetadata`/event parser 강제 + unit test
  - iter11: 2순위 확정 시 `sumMintDelta` user-account-only 필터링
  - Phase 2: 4 entries (PIPPIN×2, SWARMS×2) per-trade timeline decomposition (별도 iter)
- forbidden:
  - Phase A3 임계 [0.7, 1.3] 조정 금지 — 5×/32× inflation이 사실이면 임계 늘리는 순간 ledger 오염을 풀어주는 것
  - audit 1회 결과로 risk cooldown 완화 검토 금지 (F1-deep audit 통과 전까지)
  - 1순위 verdict 미확정 상태에서 코드 변경 금지 — iter9 IDL 확인 후에만 fix 진행

### Realized PnL (참고)

- realized_recent_7h: `-0.002876 SOL` (4 entries, 0W/4L)
- daily_limit_usage: `~6%` (제어 범위 내)
- 4 entries 100% loss 패턴은 Phase 2 per-trade decomposition에서 별도 분해 필요 (이번 audit scope 아님)

### Next Check

- next_window_utc: `2026-04-08T13:00:00Z ~ 2026-04-08T20:00:00Z` (~7h, iter9 검증 후)
- expected_change:
  - iter9 결과로 1순위 verdict 확정 또는 2순위로 이동
  - 1순위 확정 + iter10 fix 배포 시 PRICE_ANOMALY rate < 10% 기대
  - PRICE_ANOMALY rate가 그대로 유지되면 2순위/3순위로 hypothesis pivot
- fallback_if_not_improved:
  - iter9 IDL 확인이 1순위를 부정하면 즉시 2단계 (on-chain 1건 verify) 진행
  - 운영자가 1건의 PRICE_ANOMALY_BLOCK txSignature를 Solscan/Helius에서 조회 후 pre/postTokenBalances dump

## Entry 06

### Meta

- started_at_utc: `2026-04-08T01:30:00Z`
- ended_at_utc: `2026-04-08T02:10:00Z`
- operator: `igyubin` + `claude_local` (ralph-loop iter9)
- trigger: `Phase 1B step 1 — PumpSwap IDL discriminator code-only verification`
- post_deploy_session: `true`
- session_context: `Entry 05 종결 직후 ralph-loop iter9. iter8 audit doc의 1순위 후보 (parsePumpSwapInstruction offset 16 = max_sol_cost)를 PumpSwap 공식 IDL과 비교해 100% 확정 또는 부정. 코드 변경 0건, web 검색 + IDL 비교 + swapParser.ts re-read만`

### Window

- runtime_window: `n/a` (코드/IDL 비교 task, 운영 데이터 미사용)
- iter_duration_minutes: `~40`

### Evidence

- runtime_files:
  - `src/realtime/pumpSwapParser.ts` (re-read for BUY_DISCRIMINATOR confirmation)
  - `src/realtime/swapParser.ts` (re-read for parseSwapFromTransaction control flow)
- web_sources:
  - [PumpSwap IDL Gist](https://gist.github.com/Taylor123/dcd9f3285ca105efdcdf98089a2b3198) — 공식 IDL discriminator + arg 순서
  - [pump-public-docs Program Instructions](https://deepwiki.com/pump-fun/pump-public-docs/4.2-program-instructions) — `buy(base_amount_out, max_quote_amount_in, track_volume)` 시그니처
  - [pump-public-docs AMM Mechanism](https://deepwiki.com/pump-fun/pump-public-docs/4.1-pumpswap-amm-mechanism) — "instruction reverts if quote_amount_in > max_quote_amount_in"
- generated_artifact:
  - `docs/audits/price-anomaly-ratio-2026-04-08.md` updated (Phase 1B Verdict 섹션 추가)

### Verdict

**1순위 100% 확정**: `parsePumpSwapInstruction` offset 16 = `max_quote_amount_in` (slippage upper bound, user intent), NOT actual fill.

| Field | Type | Offset | 의미 |
|---|---|---:|---|
| (discriminator) | `[u8; 8]` | 0 | `[102, 6, 61, 18, 1, 218, 235, 234]` (우리 코드와 일치) |
| `base_amount_out` | u64 | 8 | 사용자가 받기를 원하는 base token lower bound |
| `max_quote_amount_in` | u64 | 16 | 사용자가 지불 의사가 있는 quote token upper bound |
| `track_volume` | OptionBool | 24+ | volume 추적 여부 |

→ 우리 코드 `parsePumpSwapInstruction`의 `priceNative = max_quote_amount_in / base_amount_out`은 **사용자의 worst-case price** (가장 나쁜 가격)이지 actual fill price가 아니다.

### 슬리피지 톨러런스 reverse-engineering

inflation 5.41× / 32.26×는 ratio `(1 + s) / (1 − s)` 패턴과 정확히 일치:

- pippin 5.41× → s ≈ 0.687 (68.7% slippage tolerance)
- swarms 32.26× → s ≈ 0.94 (94% slippage tolerance)

멤코인 트레이더는 큰 슬리피지(50~100%)를 자주 설정한다. 동일 토큰 내 success/failure interleave는 swap별 트레이더 슬리피지 차이로 정확히 설명된다.

### Code path 정밀 확인

`src/realtime/swapParser.ts:142-209` `parseSwapFromTransaction` re-read:

```typescript
const metadataAware = meta ? parseFromPoolMetadata(tx, context) : null;
if (metadataAware && isPumpSwapPool(context.poolMetadata)) {
  return metadataAware;  // (a) PumpSwap pool은 parseFromPoolMetadata 우선
}
const parsedPump = parsePumpSwapFromTransaction(tx, context);
if (parsedPump) return parsedPump;  // (b) fallback: instruction decode
```

→ `parsePumpSwapInstruction`은 **fallback path**다. `parseFromPoolMetadata`가 null을 반환할 때만 호출된다.

production에서 PRICE_ANOMALY가 100% 발생한다는 것은:

- **시나리오 X (1순위 진짜 fallback path)**: `parseFromPoolMetadata`가 null 반환 → `parsePumpSwapInstruction`이 worst-case price를 candle.close로 inject
- **시나리오 Y (2순위)**: `parseFromPoolMetadata`는 정상 동작하지만 `sumMintDelta`가 잘못된 값 산출

source label (`'transaction'`)이 같아서 X/Y 즉시 구별 불가. iter10 fix 후 PRICE_ANOMALY rate 변화로 decision tree 가동.

### Assessment

- quality_check_of_recent_answers: `pass: iter8 audit doc의 1순위 가설이 IDL과 정확히 일치. 'max_sol_cost'라는 framing은 살짝 부정확했고 실제로는 'max_quote_amount_in'이지만 의미는 동일 (slippage upper bound for quote token, which is SOL in PumpSwap)`
- primary_finding: `PumpSwap buy instruction의 두 u64 필드 모두 user intent다. instruction-decode로 priceNative를 산출하는 path는 본질적으로 잘못된 접근이다. fallback이 아니라 즉시 폐기 대상.`
- secondary_finding: `parseSwapFromTransaction은 이미 PumpSwap pool에 대해 parseFromPoolMetadata를 우선 시도하도록 되어 있지만, 어떤 이유로 null을 반환하는 경우가 100% 발생한다. iter10에서 두 가지 fix를 동시에: (1) instruction parser path 차단, (2) parseFromPoolMetadata가 null이면 swap drop`
- tertiary_finding: `이번 iter9는 web search + IDL 비교 + swapParser re-read의 read-only task. 코드 변경 0건. iter10에서 본격 fix 진행`
- summary:
  - 1순위 가설 100% 확정. iter10에서 즉시 fix code 진행 가능.
  - 시나리오 X/Y 구별은 iter10 fix 배포 후 PRICE_ANOMALY rate 변화로 결정 (decision tree).
  - iter11 (sumMintDelta filter)은 iter10 fix 후 rate 변화 없을 때만 진행.

### Action

- taken:
  - PumpSwap 공식 IDL과 우리 코드 BUY_DISCRIMINATOR 비교 — 일치 확인
  - `buy` 함수 시그니처 3개 소스에서 cross-check — `(base_amount_out, max_quote_amount_in, track_volume)` 확정
  - `src/realtime/swapParser.ts` re-read로 fallback path control flow 정밀 확인
  - `docs/audits/price-anomaly-ratio-2026-04-08.md`에 Phase 1B Verdict 섹션 추가 (iter9 IDL evidence + 시나리오 X/Y 분기 + iter10 fix 옵션 결정)
  - 본 ops-history Entry 06 작성
- deferred:
  - iter10: fix code 작성 + unit test + 배포 (다음 ralph-loop iter)
  - iter11: 시나리오 Y 확정 시 sumMintDelta filter (iter10 배포 후 PRICE_ANOMALY rate 측정 후 decision)
  - Phase 2: per-trade decomposition (별도 iter, 본 audit scope 아님)
- forbidden:
  - 1순위 확정만으로 즉시 production fix 배포 금지 — iter10에서 unit test 검증 후 배포
  - iter10 배포 전에 Phase A3 임계 조정 검토 금지

### Next Check

- next_iter: iter10 (fix code, ralph-loop 다음 turn)
- expected_change:
  - `parsePumpSwapInstruction` priceNative 산출 path 폐기
  - `parseSwapFromTransaction`에서 PumpSwap pool은 `parseFromPoolMetadata` 강제 + null 시 swap drop
  - unit test (PumpSwap buy instruction 합성 tx → priceNative null/throw)
  - `npx tsc --noEmit` + `npx jest` pass
- post_deploy_check:
  - PRICE_ANOMALY rate 변화 측정 (iter10 배포 후 7h 운영 후)
  - rate < 10% → 시나리오 X 확정 + iter10 종결
  - rate 변화 없음 → 시나리오 Y 확정 + iter11 진행

## Entry 07

### Meta

- started_at_utc: `2026-04-08T02:30:00Z`
- ended_at_utc: `2026-04-08T03:00:00Z`
- operator: `igyubin` + `claude_local` (ralph-loop iter10)
- trigger: `iter9 verdict 1순위 100% 확정 → fix code 즉시 진행`
- post_deploy_session: `false` (코드 변경, 배포는 별도)
- session_context: `Entry 06 (iter9) verdict로부터 fix 옵션 A (보수적, parser path 폐기) 확정. iter10에서 instruction parser code 자체를 삭제해 미래 재발 가능성을 0으로 만들고, parseSwapFromTransaction 분기를 단순화한다.`

### Window

- runtime_window: `n/a` (코드 변경 작업, 운영 데이터 미참조)
- code_files_modified: 3
- tests_modified: 1
- tests_added: 1 (replaces broken test)

### Evidence

- code_files_modified:
  - `src/realtime/pumpSwapParser.ts` — 157 lines → 23 lines (134 lines 삭제)
    - 삭제: `parsePumpSwapFromTransaction`, `parsePumpSwapInstruction`, `parsePumpSwapFromLogs`, `BUY_DISCRIMINATOR`, `SELL_DISCRIMINATOR`, `decodeInstructionData`, `decodeSide`, `readU64LE`, `parseNumeric`, `detectSide`
    - 삭제 import: `bs58`, `ParsedTransactionWithMeta`, `PartiallyDecodedInstruction`
    - 유지: `PUMP_SWAP_PROGRAM`, `PUMP_SWAP_DEX_IDS`, `isPumpSwapDexId`, `isPumpSwapPool`
  - `src/realtime/swapParser.ts` — `parsePumpSwapFromTransaction` import 제거, `parseSwapFromTransaction:142` 분기 단순화
    - 변경 전: PumpSwap이고 metadataAware 있으면 반환 → instruction parser 호출 → 그 외 분기
    - 변경 후: PumpSwap이면 metadataAware 그대로 반환 (null이면 swap drop) → 그 외 분기
  - `test/swapParser.test.ts` — 'parses direct PumpSwap instructions...' 테스트(line 108-170)를 'drops PumpSwap swaps when pre/post token balance deltas are missing (no instruction-decode fallback)'로 교체. 동일 입력(empty token balances + buy instruction)이 이제 `null`을 반환하는지 검증.
- verification:
  - `npx tsc --noEmit` → 0 errors
  - `npx jest test/` → **87 suites / 466 tests all pass**, swapParser 16/16 (PumpSwap tests 3/3 pass)
- consumer_audit:
  - grep `parsePumpSwap(FromTransaction|Instruction)` → src/realtime/swapParser.ts 1건 (이번 fix로 제거됨), src/realtime/pumpSwapParser.ts 정의 (이번 fix로 삭제)
  - grep `from.*pumpSwapParser` → swapParser.ts (`isPumpSwapPool`, `PUMP_SWAP_PROGRAM`), index.ts (`PUMP_SWAP_DEX_IDS`), realtimeEligibility.ts (`isPumpSwapDexId`) — 모두 유지된 export만 사용

### Verdict

- iter10 fix는 옵션 A (보수적, parser path 폐기)를 적용. 이유:
  1. PumpSwap `buy(base_amount_out, max_quote_amount_in, track_volume)` 두 u64 필드 모두 user intent로 산술적으로 actual fill 가격을 산출 불가
  2. fallback이 아니라 본질적으로 잘못된 접근이므로 코드 자체를 삭제해 미래 재발 차단
  3. 옵션 B (instrumentation) 거부 — PRICE_ANOMALY 1주일 추가 누적 비용 > 1순위 확정 verdict의 fix 비용
- 옵션 A의 부작용 (PumpSwap 일부 swap이 metadataAware null로 drop될 수 있음)은 다음 7h 모니터링에서 관측 후 시나리오 Y 분기로 처리:
  - **시나리오 X (parseFromPoolMetadata가 일관되게 동작)**: PRICE_ANOMALY rate 0%, 거래 빈도 유지 → axis_2 종결
  - **시나리오 Y (parseFromPoolMetadata가 자주 null)**: PRICE_ANOMALY rate 0%이지만 PumpSwap 거래 빈도 급감 → iter11에서 sumMintDelta user-account-only 보강

### Assessment

- quality_check_of_recent_answers: `pass: iter9 verdict 1순위 100% 확정 후 즉시 fix 작성. unit test가 broken behavior을 enshrine하던 부분(test:108-170)을 발견하고 그 자리에서 새 expectation으로 교체.`
- primary_finding: `instruction parser 코드 자체를 삭제하는 것이 옳다 — fallback 비활성화가 아니라 source code 제거로 미래 재발 가능성을 0으로 만든다.`
- secondary_finding: `swapParser.ts 분기가 의외로 단순해졌다. 변경 전에는 (a) PumpSwap+metadata → metadata, (b) instruction fallback, (c) non-PumpSwap fallback chain 순이었는데, (b)를 제거하면 PumpSwap 전체가 (a)로 합쳐지고 non-PumpSwap만 fallback chain으로 흐른다. control flow가 더 명확해짐.`
- tertiary_finding: `parsePumpSwapFromLogs도 dead code였음 (consumer 0건). decimals 미보정 + raw integer 읽기로 P0-B에서 disable됐던 함수. 이번 cleanup에서 함께 제거.`
- summary:
  - 87 suites / 466 tests all pass.
  - 코드 변경 3 files, 134 lines 삭제, 23 lines 추가 (net -111 lines).
  - 다음 단계는 VPS 배포 + 7h 운영 후 PRICE_ANOMALY rate 측정 (decision tree).

### Action

- taken:
  - `src/realtime/pumpSwapParser.ts` rewrite (157→23 lines)
  - `src/realtime/swapParser.ts` import 제거 + parseSwapFromTransaction 분기 단순화
  - `test/swapParser.test.ts` broken test 교체
  - `npx tsc --noEmit` + `npx jest test/` 전체 통과 검증
  - `CRITICAL_LIVE.md` §7H에 iter10 결과 추가
  - 본 Entry 07 작성
- deferred:
  - VPS 배포 (다음 작업)
  - 7h 운영 후 PRICE_ANOMALY rate 측정 (Entry 08)
  - iter11 sumMintDelta 보강 (시나리오 Y 확정 시에만)
- forbidden:
  - Phase A3 임계 [0.7, 1.3] 조정 — iter10 fix가 root cause를 제거했으므로 가드가 false positive였다는 가설은 폐기됨. 임계 그대로 유지.

### Next Check

- next_iter: VPS 배포 (코드 변경 없음, 운영자 작업) → 7h 후 ops loop entry로 측정
- expected_change:
  - PRICE_ANOMALY rate 100% → 0% (시나리오 X 확정)
  - PumpSwap pool 거래 빈도 (metadataAware null로 drop 비율) 측정 — drop > 50%면 시나리오 Y 보조
- post_deploy_check:
  - 7h window: signal-intents.jsonl에서 `error_code=PRICE_ANOMALY_BLOCK` 카운트
  - rate < 10% → 시나리오 X 확정, axis 종결
  - rate ≥ 50% (잔존) → 시나리오 Y 확정, iter11 sumMintDelta 보강

## Entry 08

### Meta

- started_at_utc: `2026-04-08T02:15:00Z`
- ended_at_utc: `2026-04-08T02:45:00Z`
- operator: `igyubin` + `claude_local` (ralph-loop post-review follow-up)
- trigger: `Entry 07 iter10 코드를 Codex review(/codex:review)로 감수 → P1 회귀, P2 axis 편향 발견`
- post_deploy_session: `false` (VPS 배포 전 pre-release review)
- bot_restart_at_utc: `n/a` (로컬 코드 수정만)

### Codex review findings (verbatim 요약)

1. **P1 — Don't drop PumpSwap swaps when mint deltas cancel out**
   - 위치: `src/realtime/swapParser.ts:147-154`
   - 내용: `sumMintDelta`는 동일 mint의 pre/postTokenBalances 전체 합산이다. 정상 PumpSwap fill에서 user token ATA와 pool vault는 반대 부호로 움직여 **per-mint 총합이 정확히 0**이 된다. iter10 초안의 `return metadataAware` 무조건 반환 분기가 이런 valid tx를 전부 silently discard.
2. **P2 — Exclude entries whose runner leg is still open from exit audit**
   - 위치: `scripts/analysis/exit-distribution-audit.ts:527-536`
   - 내용: `status === 'CLOSED'` 필터를 `parent_trade_id` grouping보다 먼저 적용. TP1 부분 청산 이후 parent row는 CLOSED로 닫히고 runner는 새 OPEN row로 insert되므로, parent만 남아 "완료된 TAKE_PROFIT_1 trade"로 집계됨 → Phase X3 bucket distribution 왜곡.

### P1 verification (real Solana mainnet tx)

- 대상 tx: `5dFPmLE7Gz1zNiXQR2eqyytRn3fDynmiJ2ciUd4ArBK3DJdnS2w3UJPtXuRvvFd72RzFsosNkHbeu1qmEGmDyXM2`
- pre/postTokenBalances 직접 조회 결과:
  - base mint user ATA: delta `-106_310_234_819`
  - base mint pool vault (owner=`AyDbuMurRfVKrdmv4aUAz5XmscSRwgQzsRGTCdXHzfJV`): delta `+106_310_234_819`
  - **per-mint 총합 = 0 재현 확인** → P1 회귀 확정
- pool vault의 `owner` 필드 = pool address와 정확히 일치 → owner filter 안전
- user의 wSOL은 임시 ATA(tx 내 생성/폐쇄)로 token balances에 없음 → lamports 경유, pool-only filter가 SOL 쪽에서도 단독 읽기로 일관된 결과

### P1 fix (`src/realtime/swapParser.ts`)

- `sumMintDelta(tx, mint, ownerFilter?)` — owner 필터 인자 추가. `balance.owner !== ownerFilter`인 row를 pre/post 양쪽 loop에서 skip.
- `parseFromPoolMetadata` — `isPumpSwapPool(metadata)`일 때 `context.poolAddress`를 ownerFilter로 전달. 결과는 **pool 관점 delta**이므로 `-baseDelta.amountRaw / -quoteDelta.amountRaw`로 부호 반전 후 기존 side 판정 로직 재사용.
- non-PumpSwap 풀(Raydium, Meteora 등)은 ownerFilter=undefined로 기존 동작 유지 — log 파서 경로가 이미 커버.
- Why 주석: "user token ATA + pool vault 반대 부호 → 총합 0 → null 회귀" + "user wSOL은 temp ATA라 tokenBalances에 없고 lamports 처리 → pool-only filter가 SOL 쪽에서도 일관된 결과" 명시.

### P1 tests (`test/swapParser.test.ts`)

- 교체: 기존 'prefers PumpSwap balance-delta parsing over instruction decode when both are available' (owner=`owner-1`/`owner-2`, 필터 미작동) → 'parses PumpSwap buy via pool-owned vault deltas (ignores instruction payload)'
  - user ATA entry (owner='user-1') + pool base vault entry (owner='pool-pump', pool에서 0.5 base 빠져나감) + pool quote vault entry (owner='pool-pump', 0.25 SOL 수취) 구성
  - pool 관점 base delta `-500_000`, quote delta `+250_000_000` → 부호 반전 후 user 관점 `+500_000`/`-250_000_000` → side='buy', amountBase=0.5, amountQuote=0.25, priceNative=0.5
- 신규 추가: 'parses PumpSwap sell via pool-owned vault deltas (mirror of buy case)'
  - pool base vault 1.0 → 3.0 (2 token 수취), pool quote vault 0.5 → 0.4 (0.1 SOL 송출) → user 관점 sell, amountBase=2.0, amountQuote=0.1, priceNative=0.05

### P2 fix (`scripts/analysis/exit-distribution-audit.ts`)

- `main()` 재구성 — 필터 순서 교정:
  1. `byStrategy = allTrades.filter(strategy)` — strategy 필터만 먼저
  2. `groupByParent(byStrategy)` — status 관계없이 parent 기준 grouping
  3. dirty 제외: `legs.some(exit_anomaly_reason != null)`인 group 전체 제외 (`includeDirty`로 skip 가능)
  4. open 제외: `legs.every(status === 'CLOSED')` 아닌 group 전체 제외
- `buildReport` 시그니처에 `droppedOpen` 추가. Filter 설명을 `parent group fully CLOSED AND no leg has exit_anomaly_reason`로 갱신.
- Caveats 섹션에 "Partial-exit 안전장치" + "group 단위 dirty 제외" 두 줄 추가.

### P2 verification (`data/vps-trades-latest.jsonl` dry run)

- 명령: `npx ts-node scripts/analysis/exit-distribution-audit.ts`
- 출력 헤더: `Input rows: 143, dropped (anomaly): 1, dropped (open runner leg): 4, parent groups: 134`
- 판정: **runner-open 상태 parent 4건 정확히 격리**. 이전 로직에서는 이 4건이 parent 혼자 TP1로 집계되어 전체 bucket 통계에 상향 편향을 주고 있었음.

### Verification (전체)

- `npx tsc --noEmit` → 0 errors
- `npx jest` → **87 suites / 468 tests all pass** (swapParser 15 → 17, +2 PumpSwap pool-vault buy/sell symmetry)
- audit script dry run → Filter 설명 갱신 + 4 groups 격리 확인

### Decision

- iter10 + P1/P2 fix를 **하나의 release candidate**로 합쳐서 VPS 배포 (Task #52). 별도 iter로 쪼개지 않음 — P1은 iter10 초안 자체 회귀라 분리 배포가 위험.
- Codex review 결과물은 별도 audit 문서로 persist하지 않고 Entry 08 + CRITICAL_LIVE.md §7H에만 기록. 이유: review 내용이 self-contained이고 이번 fix로 전량 closure.
- iter10 `Phase A3 임계 조정 금지` 방침 유지. P1 fix가 swap 산출을 정상화해도 abnormal PumpSwap(실제 slippage 극단)에 대한 `[0.7, 1.3]` guard 자체의 타당성은 바뀌지 않는다.

### Next actions (sequential)

1. Task #52 (iter10 + P1/P2) VPS 배포
2. 7h PRICE_ANOMALY monitoring (decision tree: 0% → 시나리오 X / 여전히 high → 시나리오 Y → iter11 추가 보강)
3. 7h 후 exit distribution audit 재실행하여 `dropped (open runner leg)` 수치가 부분 청산 트레이드 수와 일치하는지 cross-check

## Cross-references

- [`CRITICAL_LIVE.md`](../../CRITICAL_LIVE.md) §7H — Phase 1 read-only diagnosis 요약 + iter10 결과 + iter10 post-review P1/P2 fix
- [`docs/audits/price-anomaly-ratio-2026-04-08.md`](../audits/price-anomaly-ratio-2026-04-08.md) — Phase 1 audit 원문 + Phase 1B Verdict (iter9)
- [`docs/exec-plans/active/edge-cohort-quality-2026-04-07.md`](../exec-plans/active/edge-cohort-quality-2026-04-07.md) Axis 2 — 7h concentration caveat
- [`docs/ops-history/2026-04-07.md`](./2026-04-07.md) Entry 04 — signal-cohort audit (이전 iter)
